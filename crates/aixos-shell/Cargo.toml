[package]
name = "aixos-shell"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[lib]
name = "aixos_shell"
path = "src/lib.rs"

[dependencies]
aixos-kernel = { path = "../aixos-kernel" }
aixos-haniel = { path = "../aixos-haniel" }
# PL-62: vendored axon_interp (P71.5, upstream HEAD c7327f4)
# Vendored into crates/axon_interp — single source of truth.
# MAX_LINES=64, MAX_VARS=16, OUTPUT_BUF=2048, binary ops, exec_with_state()
axon_interp = { path = "../axon_interp", default-features = false }
